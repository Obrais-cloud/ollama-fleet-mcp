[project]
name = "ollama-fleet-mcp"
version = "0.1.0"
description = "MCP server exposing the local Ollama fleet (list models, generate, health) to Claude Code"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=2.0.0",
]
